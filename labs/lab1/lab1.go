package lab1

// getDuplicateCharacters returns a map of characters and number of
// their occurency, if these characters occure more than
// once in the string
func getDuplicateCharacters(input string) map[rune]int {

	frequency := make(map[rune]int)
	// use for loop with 'range' keyword to iterate over the string

	// use frequency[char] to access map values

	// look for multiple occurencies, delete character occurencies
	// delete(frequency, key)

	return frequency
}
