package lab3

/*
Write a function which returns min and max value in int slice.

Use
for _, v := range <slice_name> {
}
where range returns the key, value pair of array, slice, string or map;
"_" is a blank identifier, which discards the return value.

E.g. [0,1,2,3,4,5,6] yields (0,6,nil)
nil yields error
[] yields error
*/
func minMax(input []int) (int, int, error) {
	// if input is invalid return error
	var min, max int
	// iterate over the slice and find min/max values
	return min, max, nil
}
