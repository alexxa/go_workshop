/*
Write a function which returns max and min value in int slice.

Use 
for _, v := range <range_name> {
}
where range returns the key, value pair of array, slice, string or map;
"_" is a blank identifier, which discards the return value.
*/